<!-- Blog Posts -->
<section id="blog-posts">
    <!-- Sample Blog Post -->
    {{#each posts}}
    <article>
        <h2>{{ this.title }}</h2>
        <p>{{ this.date_created }}</p>
    </article>
    {{/each}}
</section>
